#include <stdio.h>
/**
* main - Entry point
* Description: print alphabet
* Return: Always 0 (Success)
*/
int main(void)
{
	int tracker;

	for (tracker = 0 ; tracker < 10 ; tracker++)
	{
		printf("%d", tracker);
	}
	return (0);
}
